/**
 * Centralized port configuration for Ask Jenny
 *
 * Re-exports from @ask-jenny/types for backward compatibility.
 * The canonical definition is in @ask-jenny/types to allow browser-safe imports.
 */

export { STATIC_PORT, SERVER_PORT, RESERVED_PORTS } from '@ask-jenny/types';
